# DECISIONS

重要な設計判断を、変更理由と影響が追える形で記録する。

## D-001: Google Maps JavaScript APIではなくLeafletを使用する

- 日付: 2026-08-19
- 状態: 採用
- 判断: 背景地図にOpenStreetMap、描画にLeafletを使用する。
- 理由: プロトタイプでAPIキーを不要にし、静的サイトとして早く公開するため。
- 影響: Google My Mapsを直接埋め込まず、KMLをWebアプリ側で解析する。利用規模拡大時はタイル配信方針の再検討が必要。

## D-002: 既存My Mapsの区域データをKMLとして再利用する

- 日付: 2026-08-19
- 状態: 採用
- 判断: 新しい区域境界を作らず、既存My Maps由来の `data/areas.kml` を正とする。
- 理由: 既存の区域番号、境界、色、地理的位置を維持するため。
- 影響: My Maps更新時はKMLを再取得して差し替える。WebアプリからMy Maps自体は変更しない。

## D-003: ビルド不要の静的サイトとしてGitHub Pagesで公開する

- 日付: 2026-08-19
- 状態: 採用
- 判断: HTML/CSS/JavaScriptをGitHub Pagesから直接配信する。
- 理由: LINE共有、保守、公開を簡単にし、プロトタイプの速度を優先するため。
- 影響: サーバー側で秘密情報を保持できない。機密データは静的ファイルへ入れない。

## D-004: マンションと訪問しない家を別レイヤー・別データで管理する

- 日付: 2026-08-19
- 状態: 採用
- 判断: マンションは `data/apartments.js`、訪問しない家は `data/restricted-homes.js` に分離する。
- 理由: 表示、アイコン、カード、公開情報、将来の運用が異なるため。
- 影響: レイヤー単位で表示を切り替えられる。実在する訪問しない家を扱う前に閲覧制御が必要。

## D-005: 元のKML色と業務状態色を分ける

- 日付: 2026-08-19
- 状態: 採用
- 判断: KMLの4色は区域識別色として保持し、管理対象区域だけ6色の業務状態色で上書きする。
- 理由: 元色に業務的意味がなく、状態色と混同しないようにするため。
- 影響: 状態同期対象外は元のKML色、対象区域は赤・オレンジ・黄・緑・紫・グレーで表示される。

## D-006: グループ保管を紫色とし、4か月判定から除外する

- 日付: 2026-08-19
- 状態: 暫定採用
- 判断: グループ保管を紫色で最優先表示し、長期未取扱いの赤判定を適用しない。
- 理由: グループ保管は意図的に保管している状態で、未処理とは意味が異なるため。
- 影響: 運用方針が変わる場合は数式、色判定、凡例を同時に変更する。

## D-007: 4か月判定は暦月で計算する

- 日付: 2026-08-19
- 状態: 採用
- 判断: 単純な120日ではなく、Google Sheetsの `EDATE(最終取扱日, 4)` を使う。
- 理由: 「4か月後」という運用上の理解と一致させるため。
- 影響: 最終取扱日が未記録の場合は現在「未記録」とし、自動的に赤にはしない。

## D-008: 非公開シートはApps Scriptで必要項目だけ公開する

- 日付: 2026-08-19
- 状態: 採用
- 判断: Google Sheets本体を一般公開せず、Apps Script Web APIが必要な表示項目だけを返す。
- 理由: GitHub Pagesから即時同期しつつ、担当者、日付、メモの公開を避けるため。
- 影響: APIは匿名アクセス可能だが、返却対象は現在M170、M175、M178、項目は表示用状態に限定される。Google Sheets権限は読み取り専用とする。

## D-009: JSONPでGitHub PagesとApps Scriptを接続する

- 日付: 2026-08-19
- 状態: 採用
- 判断: Apps Script APIはJSONとJSONPを返し、ブラウザ側はJSONPを使用する。
- 理由: 静的サイトからのクロスオリジン取得を、追加サーバーなしで安定させるため。
- 影響: コールバック名を正規表現で検証する。将来同一オリジンAPIやバックエンドを導入した場合は通常のJSON fetchへ移行できる。

## D-010: 同期は起動時・手動・60秒間隔とする

- 日付: 2026-08-19
- 状態: 採用
- 判断: 地図起動時、「最新情報に更新」押下時、60秒ごとに区域状態を取得する。
- 理由: プロトタイプで即時性とApps Script呼出回数のバランスを取るため。
- 影響: シート編集後は更新ボタンで即時確認できる。利用者・区域数の増加時はキャッシュや呼出間隔を再設計する。

