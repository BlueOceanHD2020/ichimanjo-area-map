# 一万城区域デジタル管理プロジェクト

## 1. プロジェクトの目的

宮崎県都城市「一万城区域」の区域管理を、紙中心の運用から地図とGoogleスプレッドシートを使った運用へ段階的に移行する。利用者がスマートフォンやLINE共有URLから地図を開き、区域・マンション・訪問しない家を確認し、必要な管理表へ迷わず移動できることを重視する。

現在は説明・検証用プロトタイプであり、ログイン、データベース、管理者画面を持たない。既存機能を維持しながら、小さな単位で実運用に近づける。

## 2. システム全体構成

```text
Google My Maps
  └─ KML（区域境界・区域名・既存色）
       ↓ ファイル差し替え
GitHub Pagesの静的Webアプリ
  ├─ Leaflet + OpenStreetMap
  ├─ 区域ポリゴンレイヤー
  ├─ マンションレイヤー
  └─ 訪問しない家レイヤー
       ↑ JSONP / 読み取り専用
Google Apps Script Web API
       ↑
Google Sheets「区域管理」
```

フロントエンドはビルド不要のHTML/CSS/JavaScriptで構成する。区域境界はKML、マンションと訪問しない家はJavaScriptデータ、区域状態はApps Script APIから取得する。

## 3. Google My Maps / KMLとの関係

- 元地図: [一万城会衆区域 My Maps](https://www.google.com/maps/d/viewer?mid=1zZCyhEM3Q4F9jEIDphr1MPPW-lFm3IE)
- 取得したKMLを `data/areas.kml` に保存している。
- KML内の区域名、ポリゴン境界、線色、塗り色、透明度をブラウザで解析して再現する。
- 現在のKMLにはM001〜M280の280区域がある。
- 元の4色は区域識別用であり、業務状態を意味しない。
- Apps Scriptから状態が返る区域は、業務状態色でKML色を一時的に上書きする。
- My Mapsを更新した場合は、再取得したKMLで `data/areas.kml` を差し替える。My Maps自体はWebアプリから変更しない。

## 4. GitHub Pages

- リポジトリ: [BlueOceanHD2020/ichimanjo-area-map](https://github.com/BlueOceanHD2020/ichimanjo-area-map)
- 公開URL: [一万城区域マップ](https://blueoceanhd2020.github.io/ichimanjo-area-map/)
- `main` ブランチへのpush後、GitHub Pagesが静的ファイルを公開する。
- APIキーやビルド処理は不要。
- LINE内ブラウザ、iPhone Safari、Chromeを主な対象とする。
- キャッシュ更新時はCSS/JavaScript URLのクエリバージョンも更新する。

## 5. Google Sheets

使用中のスプレッドシート:

[一万城区域_マンション管理プロトタイプ_サンライズ平江](https://docs.google.com/spreadsheets/d/1tZfA5jLAPCSc-gnoO31lYs5wJj3l2W8K3bZvxZ18alo/edit)

| タブ | gid | 用途 |
|---|---:|---|
| マンション一覧 | 1693332196 | マンション単位の情報 |
| 部屋管理 | 2001 | 101〜303号室の訪問状況、午前・午後チェックボックス |
| MyMaps取込 | 2002 | My Maps関連データの取込用 |
| 区域管理 | 2003 | M001〜M280の保管・貸出・進捗・日付・表示色 |

「区域管理」はネイティブGoogle Sheetsテーブルで、保管区分、貸出状態、進捗にプルダウンを設定している。最終取扱日から暦上4か月が経過したかを数式で判定する。

## 6. Apps Script API

- ソース: `apps-script/Code.gs`
- マニフェスト: `apps-script/appsscript.json`
- clasp設定: `.clasp.json`
- 実行方式: デプロイ所有者として実行する匿名アクセス可能なWebアプリ。
- Google Sheets APIの読み取り専用スコープだけを使用する。
- JSONとJSONPに対応し、GitHub PagesからはJSONPで取得する。
- 現在APIが返す対象はM170、M175、M178のみ。
- 返却項目は区域ID、保管区分、貸出状態、進捗、経過判定、表示色に限定する。
- 担当者、日付、メモなどは公開APIへ出さない。

## 7. 区域管理

区域状態の基本色は次の6色とする。

| 色 | 意味 |
|---|---|
| 赤 | 最終取扱日から4か月以上経過 |
| オレンジ | 貸出中・未完了 |
| 黄 | 未完了 |
| 緑 | 完了 |
| 紫 | グループ保管 |
| グレー | 通常、未貸出、未着手 |

判定優先順位は、グループ保管、4か月超、貸出中、未完了、通常の順。グループ保管は意図的な保管であるため、現状は4か月判定の対象外とする。

連携確認用の状態:

- M170: 紫、グループ保管
- M175: オレンジ、貸出中・未完了
- M178: 赤、4か月超

対象区域をタップすると状態カードが開き、区域管理シートの該当行へ移動できる。

## 8. マンション管理

- データ: `data/apartments.js`
- 現在の対象: `427-M01｜サンライズ平江`
- 住所: 宮崎県都城市平江町6-12
- 部屋数: 9戸（101〜303）
- 地図上では赤いマンションピンで表示する。
- ピンのカードからGoogle Sheets「部屋管理」へ移動する。
- マンションを増やす場合は配列へ同じ形式のオブジェクトを追加する。

## 9. 訪問しない家管理

- データ: `data/restricted-homes.js`
- 画像: `assets/restricted-home-sample.png`
- 現在の対象: `427-R01` の架空サンプル1件。
- 地図上では黒い「×」ピンで表示する。
- ピンを押すと、建物確認画像、目印、訪問しない旨を表示する。
- 現在の画像はAI生成した架空住宅で、実在住所とは関係しない。
- 実データ化する場合は、位置・画像・注意事項の公開範囲を再検討する。

## 10. レイヤー表示切替

画面下部で以下を切り替えられる。

- すべてのピン
- マンションだけ
- 訪問しない家だけ
- 区域境界のON/OFF

区域ポリゴン、マンション、訪問しない家は別々のLeaflet LayerGroupとして管理する。

## 11. シートとの同期方法

1. Google Sheets「区域管理」を編集する。
2. シート内の数式が経過判定と表示色を更新する。
3. Apps Script APIがM170、M175、M178の表示用項目を読み取る。
4. 地図は起動時、更新ボタン押下時、60秒ごとにAPIを呼ぶ。
5. 対象ポリゴンの色、ツールチップ、ポップアップ内容を更新する。

`data/area-statuses.js` はAPI取得前や障害時に使う初期スナップショット兼設定であり、各区域のシート行番号とAPI URLを保持する。

## 12. 公開情報と非公開情報

### 公開情報

- GitHub Pagesの全ファイル
- GitHub公開リポジトリのソースコード
- `data/areas.kml` の区域境界・区域名・既存色
- マンションサンプルとGoogle Sheetsへのリンク
- 架空の訪問しない家サンプル
- APIが返す3区域の保管区分、貸出状態、進捗、経過判定、表示色

### 非公開または権限で保護する情報

- Googleスプレッドシート本体の内容（Google側の共有権限に従う）
- APIから除外した担当者、日付、メモ
- 将来登録する実在の訪問拒否宅の詳細
- Google認証トークンや個人認証情報。これらはリポジトリへ保存しない。

## 13. 現在実装済み機能

- モバイル優先のレスポンシブ地図UI
- Leaflet + OpenStreetMap背景地図
- My Maps由来KMLの280区域表示
- KMLの区域名、境界、色、透明度の再現
- マンションピンと部屋管理シートへのリンク
- 写真付き「訪問しない家」サンプルピン
- ピン種別と区域境界の表示切替
- Google Sheets「区域管理」テーブル
- 6色の区域状態判定
- 4か月経過の自動判定
- M170、M175、M178のApps Script即時同期
- 起動時、手動、60秒間隔の更新
- 区域カードから管理表の該当行を開く導線
- GitHub Pagesへの自動公開

## 14. 未実装機能

- M001〜M280すべてのApps Script API同期
- Web地図からの区域状態編集
- 区域の貸出・返却ワークフロー
- グループ保管区域の本番データ登録
- 複数マンションの本番登録
- 実在する訪問しない家の安全な登録・閲覧制御
- ログイン、権限管理、監査ログ
- 管理者画面
- 検索、担当者絞り込み、状態別区域フィルター
- KML更新の自動取込
- 自動テストと継続的なブラウザ検証
- オフライン対応

## 15. 今後のロードマップ

### フェーズ1: プロトタイプ完成度向上

- 3区域の即時同期を実機で検証する。
- 状態別フィルターと凡例を改善する。
- 色判定ルールとグループ保管ルールを関係者で確定する。

### フェーズ2: 区域管理の拡張

- API対象をM001〜M280へ拡張する。
- 区域検索、貸出中、未完了、4か月超、グループ保管の絞り込みを追加する。
- 貸出・返却履歴と担当者運用を整備する。

### フェーズ3: 建物管理の拡張

- マンションを複数登録し、部屋管理シートとの関連付けを標準化する。
- 訪問しない家の画像登録手順と公開範囲を確定する。
- 必要に応じて認証を導入する。

### フェーズ4: 運用基盤

- KML差し替え手順を自動化する。
- バックアップ、監査、障害時の復旧手順を整備する。
- 数百区域・多数マンションを前提に性能とデータ構造を見直す。

## 文書管理ルール

- 新機能を追加したら `CHANGELOG.md` と `TODO.md` を同じ変更で更新する。
- 重要な設計判断や既存判断の変更は `DECISIONS.md` に理由と影響を記録する。
- 全体目的や構成が変わったら本ファイルも更新する。

